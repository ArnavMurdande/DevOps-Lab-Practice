pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                // Add your build steps here (e.g., Maven, Gradle, npm commands)
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add your test steps here (e.g., JUnit, Selenium tests)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
                // Add your deployment steps here (e.g., deploy to a web server, cloud platform)
            }
        }
    }
}
